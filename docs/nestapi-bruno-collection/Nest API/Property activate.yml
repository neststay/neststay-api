info:
  name: Property activate
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/properties/:slug/activate"
  headers:
    - name: accept
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: slug
      value: 01KYQ7RF9HCY298WN6C2EX8GBG
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
