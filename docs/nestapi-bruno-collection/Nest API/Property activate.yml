info:
  name: Property activate
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/properties/:slug/activate"
  headers:
    - name: accept
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: slug
      value: 01KZ0XYDTVZD1W12H2X712SD0Y
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
