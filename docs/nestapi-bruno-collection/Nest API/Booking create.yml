info:
  name: Booking create
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseUrl}}/bookings"
  headers:
    - name: accept
      value: application/json
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "propertySlug": "01KZ0XYDTVZD1W12H2X712SD0Y",
        "checkInDate": "2026-09-10",
        "checkOutDate": "2026-09-15"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
