info:
  name: Property deactivate
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/properties/:slug/deactivate"
  headers:
    - name: accept
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: slug
      value: 01KZ0XYDTVZD1W12H2X712SD0Y
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
